#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Copy this file to credentials.h and fill in your WiFi credentials
// credentials.h is gitignored and will not be committed

const char* WIFI_SSID = "your-wifi-ssid";
const char* WIFI_USER = "your-username";  // For WPA2 Enterprise (eduroam)
const char* WIFI_PASSWORD = "your-password";
const bool USE_ENTERPRISE = true;  // Set to false for regular WiFi

#endif
